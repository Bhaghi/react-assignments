
Questions

1.  What is React? Ans. React is a library that is used in JavaScript
    .By using this library we can build our user interface .The best
    part of react is that it is a single page appaications which means
    when user update some sections in code without reloading the entire
    page.eg .while watching the reels on Instagram ,when we reload it,it
    will reload only the reel section not entire Instagram.

2.  Who developed React? Ans. React was developed by Jordan Walke in
    2013

3.  Is React a library, framework or programming language? Ans. React is
    a library that is uses in JavaScript .

4.  What is a user interface? Ans. User Interface is a part of
    application that user see and interact with

5.  What is a Single Page Application? Ans. Single Page Application
    means websites that update the content without reloading the entire
    page.

6.  What is a component in React? Ans Component is like building ui into
    small pieces.One component can be use in many places

7.  Why are components useful? Ans. Components are useful because
    instead of writing large no of code make small component and we can
    reuse that component.

8.  What is JSX? Ans. JSX is a JavaScript XML .where its like html code
    wriiten inside javascript.

9.  What is the DOM? Ans. DOM stands for Document Object Model,its like
    a tree structure of our html

10. What is the Virtual DOM? Ans. Virtual DOM is nothing but react
    creates a copy of the real DOM in memory,this copy is called Virtual
    DOM.When something changes,React compares the virtual DOM with the
    real DOM and only updates the parts that actually changed.

11. What is the difference between the real DOM and the Virtual DOM?
    Ans. The difference between the real DOM and the Virtual DOM is that
    real DOM is the tree structure of html

12. Write any three advantages of React. Ans 1.we can reuse the
    component . 2.It is easy to build fast and interactive user
    interface 3.it improves the performance performance by using Virtual
    DOM

13. Where can React be used? Ans. React can be used in building SPA web
    application.Developing dynamic websites.

14. Why is React called declarative? Ans React is called declarative
    because developers describe what the UI should look like for a given
    state, and React automatically updates the user interface when the
    data changes.

15. Why is React popular among web developers? Ans. React is popular
    among web developers because its easy to learn and maintain, Uses
    reusable components,
